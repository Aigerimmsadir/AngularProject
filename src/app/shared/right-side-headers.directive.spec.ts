import { RightSideHeadersDirective } from './right-side-headers.directive';

describe('RightSideHeadersDirective', () => {
  it('should create an instance', () => {
    const directive = new RightSideHeadersDirective();
    expect(directive).toBeTruthy();
  });
});

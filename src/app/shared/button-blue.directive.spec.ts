import {ButtonBlueDirective} from './button-blue.directive';

describe('ButtonBlueDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonBlueDirective();
    expect(directive).toBeTruthy();
  });
});
